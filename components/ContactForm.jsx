'use client';
import {useState} from 'react';
export default function ContactForm(){const [status,setStatus]=useState(''),[busy,setBusy]=useState(false);async function send(e){e.preventDefault();const form=e.currentTarget;const f=new FormData(form);setBusy(true);try{const r=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:f.get('name'),email:f.get('email'),phone:f.get('phone'),message:f.get('message'),website:f.get('website'),consent:f.get('consent')==='on'})});const d=await r.json();setStatus(r.ok?'Mesajınız büromuza ulaştı. Teşekkür ederiz.':d.error);if(r.ok)form.reset();}catch{setStatus('Gönderilemedi. Lütfen tekrar deneyin.');}setBusy(false);}return <form className="form" onSubmit={send}><span className="eyebrow">Mesaj Gönder</span><h2>Kısa bir not bırakın</h2><label>Ad Soyad<input name="name" minLength={2} maxLength={100} required autoComplete="name"/></label><div className="formRow"><label>Telefon<input name="phone" maxLength={30} autoComplete="tel"/></label><label>E-posta<input name="email" type="email" maxLength={254} required autoComplete="email"/></label></div><label className="honeypot" aria-hidden="true">Web sitesi<input name="website" tabIndex={-1} autoComplete="off"/></label><label>Mesaj<textarea name="message" rows={6} minLength={10} maxLength={5000} required/></label><label className="check"><input type="checkbox" name="consent" required/> İletişim bilgilerimin talebime yanıt vermek amacıyla işlenmesini kabul ediyorum.</label><p><a href="/gizlilik">Gizlilik açıklamasını okuyun.</a> Lütfen özel nitelikli kişisel veri veya dosya belgesi göndermeyin.</p><button disabled={busy} className="btn gold">{busy?'Gönderiliyor…':'Mesajı Gönder ↗'}</button><p role="status">{status}</p></form>;}
